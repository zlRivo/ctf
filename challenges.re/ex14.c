#include <stdio.h>

// Returns whether the given char is a letter
int isletter(char c) {
    return ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
}

int main(int argc, char** argv) {
    return 0;
}